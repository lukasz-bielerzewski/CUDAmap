#include "cuMap3D.h"

cuMap3D::cuMap3D(int _mapSize, double resolution, double _raytraceFactor, int _pointThreshold)
{
    // pointThreshold = _pointThreshold;
    // mapSize = _mapSize;
    // mapResolution = resolution;
    // raytraceFactor = _raytraceFactor;
    // xmin = ymin = zmin = -1.0 * mapSize * resolution/2.0;
    // xmax = ymax = zmax = -1.0* xmin;
    // indexes.clear();
    // raytracing = true;
    // limitUpdates = false;
    // probabilityThreshold = 0.153;
}
